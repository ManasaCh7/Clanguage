#include<stdio.h>
int main()
{
    int i,a,count;
    printf("enter any num");
    scanf("%d",&a);
    for(i=2;i<a;i++)
    {
        if(a%i==0)
        {
            count++;
        }
    }
    if(count==0)
    {
        printf("it is prime num");
    }
    else
    printf( "it is not prime");
}
